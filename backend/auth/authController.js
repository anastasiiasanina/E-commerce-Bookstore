const bcrypt = require('bcryptjs');

const registration = () => {
  try {
    
  } catch (error) {
    res.status(400).json({ message: 'Error found' });
  }
}

const login = () => {
  try {
    
  } catch (error) {
    res.status(400).json({ message: 'Error found' });
  }
}

module.exports = {
  registration,
  login
}